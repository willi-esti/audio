websockets
openai-whisper # Or whisper if you installed the non-openai fork
numpy
ffmpeg-python
aiohttp
TTS # Coqui TTS wrapper
# Optional: If using GPU for TTS/Whisper, you might need specific torch version
# torch --extra-index-url https://download.pytorch.org/whl/cu118 # Example for CUDA 11.8